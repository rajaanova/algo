package main

import (
	"fmt"
)

func main() {
	//key := [256]byte{}
	//_, err := rand.Read(key[:])
	//if err != nil {
	//	panic(err)
	//}
	//j := key[:]
	//fmt.Println(base64.URLEncoding.EncodeToString(j))
	var a string
	fmt.Scanf("%s", &a)
	fmt.Println(a)
	//fmt.Println(string(j))
}
